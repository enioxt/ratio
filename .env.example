# Copy this file to .env in project root.
# EGOS members: run `bash ~/.egos/bin/sync-env.sh $(pwd)` to auto-populate from canonical vault (~/.egos/secrets.env)

# Required for current pipeline
GEMINI_API_KEY="your_gemini_api_key_here"
API_CORS_ORIGINS="http://127.0.0.1:5500,http://localhost:5500"

# Embedding configuration
EMBED_MODEL="gemini-embedding-001"
EMBED_BATCH_SIZE="100"
EMBED_DELAY="0.0"

# Retrieval and ranking
TOPK_HYBRID="80"
TOPK_RERANK="11"
SEMANTIC_WEIGHT="0.45"
LEXICAL_WEIGHT="0.20"
RECENCY_WEIGHT="0.35"
RECENCY_HALFLIFE_YEARS="7.0"

# 1 = prioritize recency, 0 = disable
PREFER_RECENT_DEFAULT="1"

# Response generation
GENERATION_MODEL="gemini-3-flash-preview"
GENERATION_FALLBACK_MODEL="gemini-2.5-flash"
GENERATION_MAX_OUTPUT_TOKENS="3600"
GENERATION_THINKING_BUDGET="128"

# ── Guard Brasil PII Protection (LGPD art. 33 compliance) ──────────────────────
# Set RATIO_PII_GUARD_ENABLED=1 to mask Brazilian PII (CPF, RG, phone, etc.)
# before any text reaches an external LLM provider.
# Get Guard Brasil free tier at https://guard.egos.ia.br
# When disabled (default): zero latency, zero cost, no behaviour change.
RATIO_PII_GUARD_ENABLED=0
GUARD_BRASIL_URL=https://guard.egos.ia.br

# ── Generation provider ─────────────────────────────────────────────────────────
# Generation provider: "gemini" (default), "claude", "openrouter", or "alibaba"
# - gemini:     requires GEMINI_API_KEY (default)
# - claude:     requires ANTHROPIC_API_KEY — best quality for PT-BR legal text
# - openrouter: requires OPENROUTER_API_KEY — single key, 200+ models, free tier
# - alibaba:    requires ALIBABA_DASHSCOPE_API_KEY — Qwen family, great cost/quality
GENERATION_PROVIDER="gemini"

# Optional: Claude API key for generation (alternative to Gemini)
# Get your key at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=""

# Optional: OpenRouter API key — single key for 200+ models
# Create a free account and get your key at https://openrouter.ai/keys
# Then set GENERATION_PROVIDER=openrouter above.
OPENROUTER_API_KEY=""

# Optional: override the OpenRouter model (default: deepseek/deepseek-chat-v3-0324:free)
# Recommended cost-effective options:
#   qwen/qwen3-235b-a22b:free          — free tier, 1M context
#   deepseek/deepseek-r1:free          — free tier, strong reasoning
#   deepseek/deepseek-chat-v3-0324:free — free tier, excellent quality
#   minimax/minimax-01                  — $0.30/$1.20 per 1M tokens
#   moonshot/kimi-k2                    — $0.60/$3.00 per 1M tokens
# OPENROUTER_MODEL="deepseek/deepseek-chat-v3-0324:free"

# Optional: Alibaba DashScope API key (alternative to Gemini/Claude/OpenRouter)
# Get your key at https://dashscope.console.aliyun.com/apiKey
# Uses the OpenAI-compatible endpoint — excellent Qwen models at low cost.
ALIBABA_DASHSCOPE_API_KEY=""
ALIBABA_DASHSCOPE_BASE_URL="https://dashscope-intl.aliyuncs.com/compatible-mode/v1"
# ALIBABA_MODEL="qwen-plus"  # qwen-turbo (fast/cheap) | qwen-plus | qwen-max

# Optional: Claude model overrides for the Escritorio pipeline.
# Only used when GENERATION_PROVIDER=claude.
# Defaults shown below map to the closest-capability Claude models.
# RATIO_ESCRITORIO_CLAUDE_MODEL_REASONING="claude-sonnet-4-20250514"
# RATIO_ESCRITORIO_CLAUDE_MODEL_PESQUISADOR="claude-haiku-4-5-20251001"

# Text-to-Speech (TTS)
# gemini_native (default): Gemini native audio path
# legacy_google: Google Cloud Text-to-Speech Neural2 (rollback path)
TTS_PROVIDER="gemini_native"

# Legacy Google TTS profile (default rollback path)
GOOGLE_TTS_API_KEY=""
GOOGLE_TTS_VOICE_NAME="pt-BR-Neural2-B"
GOOGLE_TTS_MAX_CHARS="5000"
GOOGLE_TTS_REQUEST_TIMEOUT_MS="45000"

# Gemini native TTS profile (optional)
GEMINI_TTS_MODEL="gemini-2.5-pro-preview-tts"
GEMINI_TTS_VOICE="charon"
GEMINI_TTS_MAX_CHARS="5000"
GEMINI_TTS_STREAM_MAX_CHARS="1200"
GEMINI_TTS_REQUEST_TIMEOUT_MS="45000"
GEMINI_TTS_REQUEST_RETRY_ATTEMPTS="0"
GEMINI_TTS_MODEL_MAX_ATTEMPTS="1"
GEMINI_TTS_PREFETCH_CONCURRENCY="1"

# Context assembly for richer answers
CONTEXT_MAX_PASSAGES_PER_DOC="5"
CONTEXT_MAX_PASSAGE_CHARS="1000"
CONTEXT_MAX_DOC_CHARS="2500"

# Automatic jurisprudence update (STF/STJ)
JURIS_UPDATE_TARGET_YEAR="2026"
JURIS_UPDATE_INCLUDE_STF="1"
JURIS_UPDATE_INCLUDE_STJ="1"
JURIS_UPDATE_VISIBLE_BROWSER="1"
JURIS_UPDATE_EMBED_MODEL="gemini-embedding-001"
JURIS_UPDATE_EMBED_BATCH_SIZE="16"
JURIS_UPDATE_STJ_REPAIR_WITH_GEMINI="1"
JURIS_UPDATE_STJ_REPAIR_MODEL="gemini-3-flash-preview"
JURIS_UPDATE_STJ_REPAIR_MIN_CONFIDENCE="0.62"
JURIS_UPDATE_STJ_REPAIR_MAX_RECORDS_PER_PDF="24"
JURIS_UPDATE_STRICT_COMPLETENESS="1"
JURIS_UPDATE_EMBED_FALLBACK_ON_QUOTA="1"
JURIS_UPDATE_JOB_POLL_MS="1500"
RATE_LIMIT_JURIS_UPDATE_PER_HOUR="4"

# Optional explicit Chromium executable for STF collection.
# Leave empty when using bundled Playwright browser.
PLAYWRIGHT_CHROMIUM_EXECUTABLE=""
